apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: dev-platform-infra
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'https://github.com/VK-Paints/vkpaints-helm-charts.git'
    targetRevision: dev
    path: charts/platform-infra
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: vkpaints-dev-ns
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - CreateNamespace=true
